<script setup lang="ts"></script>

<template>
  <!-- HeroBase: key-art comes from the global BgLayer; readability/darkening is
       handled entirely by the app-level .top-fade/.bottom-fade (no per-view
       scrim). No title overlay (per P1 spec). Countdown pill and NewsPanel are
       deferred to P2. BottomBar/HomeActionBar floats at the bottom (App.vue). -->
  <div class="view view-detail"></div>
</template>
