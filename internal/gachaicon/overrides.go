package gachaicon

// applyOverrides injects curated name→Entry fixes (player-named units, bracketing,
// official-繁中 vs Hakush word-choice) into a freshly built index. Populated after
// the human reviews the match-rate miss list. Empty for now.
func applyOverrides(idx *Index) {}
